function arrPrint(arr){
    for (let i = arr.length-1; i>=0; i--){
      console.log(arr[i])
    }
    return
  }
  
  // const arrPrint = (arr) =>{
  //   arr.reverse().forEach(e=>{
  //     console.log(e);
  //   })
  // }
  
  
  arrPrint(['whisky', 'blanco', 'keto', 'teot', 'yuju'])